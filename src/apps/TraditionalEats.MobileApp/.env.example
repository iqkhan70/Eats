# Copy to .env and fill in your values.
# .env is gitignored – never commit secrets.

# Google Sign-In (required for "Continue with Google")
# 1. Create OAuth 2.0 credentials at https://console.cloud.google.com/
# 2. Create an "iOS" client (for native) or "Web application" (for redirect flow)
# 3. For iOS: use the iOS client ID (ends with .apps.googleusercontent.com)
# 4. For web/Expo: add redirect URI (e.g. https://auth.expo.io/@your-username/kram-mobile)
EXPO_PUBLIC_GOOGLE_CLIENT_ID=
